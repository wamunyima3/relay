{
  "name": "relay-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Relay — port AI coding conversations between tools. Monorepo: engine + interactive CLI.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "relay": "pnpm --filter @relay/core relay",
    "ui": "pnpm --filter @relay/cli dev"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
